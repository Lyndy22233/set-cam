# 🎨 Visual System Flow Guide

This document provides visual representations of how the Smoke Emission Test Center Appointment System works.

---

## 🗺️ System Overview Map

```
┌─────────────────────────────────────────────────────────────────┐
│                    SMOKE EMISSION TEST CENTER                    │
│                    APPOINTMENT SYSTEM                            │
└─────────────────────────────────────────────────────────────────┘
                              │
              ┌───────────────┴───────────────┐
              │                               │
        ┌─────▼─────┐                  ┌─────▼─────┐
        │   USERS   │                  │  ADMINS   │
        └───────────┘                  └───────────┘
              │                               │
    ┌─────────┼─────────┐          ┌─────────┼─────────┐
    │         │         │          │         │         │
┌───▼───┐ ┌──▼──┐ ┌───▼───┐  ┌───▼───┐ ┌──▼──┐ ┌───▼───┐
│Browse │ │Book │ │ Track │  │Review │ │Test │ │Manage │
│Service│ │Appt │ │Status │  │Appts  │ │Entry│ │System │
└───────┘ └─────┘ └───────┘  └───────┘ └─────┘ └───────┘
```

---

## 👤 User Journey Flow

### Complete User Experience

```
START
  │
  ▼
┌──────────────┐
│   Landing    │ ◄── Home Page (Features, How It Works)
│     Page     │
└──────┬───────┘
       │
       ▼
┌──────────────┐
│   Register/  │ ◄── New User → Register
│    Login     │     Existing → Login
└──────┬───────┘
       │
       ▼
┌──────────────┐
│    Browse    │ ◄── View all available services
│   Services   │     See prices, durations
└──────┬───────┘
       │
       ▼
┌──────────────┐
│    Select    │ ◄── Click "Book Now" on service
│   Service    │
└──────┬───────┘
       │
       ▼
┌──────────────┐
│ Fill Booking │ ◄── Vehicle info, Date/Time,
│     Form     │     Payment method
└──────┬───────┘
       │
       ▼
┌──────────────┐
│    Upload    │ ◄── Upload payment receipt
│   Receipt    │     (can be done later)
└──────┬───────┘
       │
       ▼
┌──────────────┐
│ Appointment  │ ◄── Status: Pending Verification
│   Created    │
└──────┬───────┘
       │
       ▼
┌──────────────┐
│     Wait     │ ◄── Admin reviews appointment
│  for Admin   │     Status updates automatically
│   Review     │
└──────┬───────┘
       │
       ├───► Rejected? → See reason, can book again
       │
       ▼
┌──────────────┐
│   Approved   │ ◄── Status: Approved
│ Appointment  │     Wait for test date
└──────┬───────┘
       │
       ▼
┌──────────────┐
│    Attend    │ ◄── Go to test center on date
│     Test     │
└──────┬───────┘
       │
       ▼
┌──────────────┐
│   Receive    │ ◄── Admin adds test results
│   Results    │     Status: Completed
└──────┬───────┘
       │
       ▼
┌──────────────┐
│    Print     │ ◄── Download/Print certificate
│  Certificate │
└──────────────┘
       │
       ▼
      END
```

---

## 👨‍💼 Admin Workflow

### Appointment Management Process

```
ADMIN LOGIN
     │
     ▼
┌─────────────┐
│   Admin     │ ◄── View statistics dashboard
│  Dashboard  │     See total, pending, approved
└──────┬──────┘
       │
       ▼
┌─────────────┐
│    View     │ ◄── List of all appointments
│Appointments │     Filter by status
└──────┬──────┘
       │
       ▼
┌─────────────┐
│   Select    │ ◄── Click on appointment
│Appointment  │     View full details
└──────┬──────┘
       │
       ▼
┌─────────────┐
│    View     │ ◄── Check uploaded receipt
│   Receipt   │     Verify payment
└──────┬──────┘
       │
       ├───────────────┐
       │               │
       ▼               ▼
┌──────────┐    ┌──────────┐
│ Approve  │    │  Reject  │
│          │    │ (reason) │
└────┬─────┘    └────┬─────┘
     │               │
     │               ▼
     │        ┌─────────────┐
     │        │User notified│
     │        │Can see      │
     │        │reason       │
     │        └─────────────┘
     │
     ▼
┌─────────────┐
│    Wait     │ ◄── User attends test
│  for Test   │
│    Date     │
└──────┬──────┘
       │
       ▼
┌─────────────┐
│  Conduct    │ ◄── Perform smoke emission test
│    Test     │     Measure CO2, opacity
└──────┬──────┘
       │
       ▼
┌─────────────┐
│    Enter    │ ◄── Input test measurements
│   Results   │     Pass or Fail result
└──────┬──────┘
       │
       ▼
┌─────────────┐
│  Complete   │ ◄── Status: Completed
│Appointment  │     User can view/print
└─────────────┘
```

---

## 📊 Appointment Status Flow

### Status Progression Diagram

```
┌─────────────┐
│   PENDING   │ ◄── Initial state after booking
└──────┬──────┘     (No receipt uploaded)
       │
       │ User uploads receipt
       │
       ▼
┌─────────────┐
│  PENDING    │ ◄── Waiting for admin review
│VERIFICATION │     (Receipt uploaded)
└──────┬──────┘
       │
       ├───────────────────┐
       │                   │
       │ Admin approves    │ Admin rejects
       │                   │
       ▼                   ▼
┌─────────────┐      ┌─────────────┐
│  APPROVED   │      │  REJECTED   │
└──────┬──────┘      └─────────────┘
       │                   │
       │                   │ User can book again
       │                   │
       │ Admin adds        ▼
       │ test result    [END]
       │
       ▼
┌─────────────┐
│  COMPLETED  │ ◄── Final state
└─────────────┘     (Results available)
       │
       ▼
   [END]
```

### Status Colors

```
🟡 PENDING              - Yellow (Awaiting receipt)
🟡 PENDING VERIFICATION - Yellow (Awaiting admin)
🟢 APPROVED             - Green (Confirmed)
🔴 REJECTED             - Red (Not approved)
🔵 COMPLETED            - Blue (Finished)
```

---

## 💾 Data Flow Diagram

### How Data Moves Through the System

```
┌────────────┐
│   USER     │
│  BROWSER   │
└─────┬──────┘
      │
      │ 1. Register/Login
      │
      ▼
┌────────────┐
│  Firebase  │ ◄── Authentication
│    Auth    │
└─────┬──────┘
      │
      │ 2. JWT Token
      │
      ▼
┌────────────┐
│   React    │ ◄── Store user state
│   App      │     Show protected routes
└─────┬──────┘
      │
      │ 3. API Request + Token
      │
      ▼
┌────────────┐
│  Express   │ ◄── Verify token
│   Backend  │     Check permissions
└─────┬──────┘
      │
      ├───────────────┬───────────────┐
      │               │               │
      ▼               ▼               ▼
┌──────────┐   ┌──────────┐   ┌──────────┐
│Firestore │   │ Firebase │   │ Firebase │
│ Database │   │ Storage  │   │  Admin   │
└────┬─────┘   └────┬─────┘   └────┬─────┘
     │              │              │
     │ 4. Query     │ 4. Upload    │ 4. Set
     │    Data      │    Files     │    Claims
     │              │              │
     ▼              ▼              ▼
┌────────────┐   ┌────────────┐   ┌────────────┐
│  Return    │   │  Return    │   │   Update   │
│   Data     │   │    URL     │   │    User    │
└─────┬──────┘   └─────┬──────┘   └─────┬──────┘
      │                │                │
      └────────────────┴────────────────┘
                       │
                       │ 5. Response
                       │
                       ▼
              ┌────────────┐
              │   React    │
              │    App     │
              └─────┬──────┘
                    │
                    │ 6. Update UI
                    │
                    ▼
              ┌────────────┐
              │   USER     │
              │   SEES     │
              │  RESULTS   │
              └────────────┘
```

---

## 🔐 Authentication Flow

### User Authentication Process

```
USER
  │
  │ Enters credentials
  │
  ▼
┌──────────────┐
│    React     │
│  Login Form  │
└──────┬───────┘
       │
       │ Email + Password
       │
       ▼
┌──────────────┐
│   Firebase   │
│     Auth     │
└──────┬───────┘
       │
       ├──── Invalid ──►┌──────────┐
       │                │  Error   │
       │                │ Message  │
       │                └──────────┘
       │
       │ Valid credentials
       │
       ▼
┌──────────────┐
│  JWT Token   │
│  Generated   │
└──────┬───────┘
       │
       ▼
┌──────────────┐
│    Store     │
│  in React    │
│    State     │
└──────┬───────┘
       │
       │ Include in API requests
       │
       ▼
┌──────────────┐
│   Backend    │
│  Middleware  │
│   Verifies   │
└──────┬───────┘
       │
       ├──── Invalid ──►┌──────────┐
       │                │   401    │
       │                │Unauthorized
       │                └──────────┘
       │
       │ Valid token
       │
       ▼
┌──────────────┐
│   Access     │
│  Granted     │
└──────────────┘
```

---

## 📁 File Upload Flow

### Receipt Upload Process

```
USER
  │
  │ Selects image file
  │
  ▼
┌──────────────┐
│   Browser    │
│ File Input   │
└──────┬───────┘
       │
       │ File selected
       │
       ▼
┌──────────────┐
│    React     │
│  Component   │
└──────┬───────┘
       │
       │ Click upload
       │
       ▼
┌──────────────┐
│   Firebase   │
│   Storage    │
│    Upload    │
└──────┬───────┘
       │
       │ Upload progress
       │
       ▼
┌──────────────┐
│     File     │
│   Stored     │
└──────┬───────┘
       │
       │ Get download URL
       │
       ▼
┌──────────────┐
│     Send     │
│   URL to     │
│   Backend    │
└──────┬───────┘
       │
       │ URL + Appointment ID
       │
       ▼
┌──────────────┐
│   Update     │
│  Firestore   │
│   Record     │
└──────┬───────┘
       │
       │ Status updated
       │
       ▼
┌──────────────┐
│    User      │
│    Sees      │
│  "Pending    │
│Verification" │
└──────────────┘
```

---

## 🗃️ Database Structure

### Firestore Collections

```
FIRESTORE DATABASE
│
├── users/
│   └── {userId}/
│       ├── name: string
│       ├── email: string
│       ├── phone: string
│       ├── role: string
│       └── createdAt: timestamp
│
├── services/
│   └── {serviceId}/
│       ├── name: string
│       ├── description: string
│       ├── price: number
│       ├── duration: string
│       └── category: string
│
└── appointments/
    └── {appointmentId}/
        ├── userId: string
        ├── userEmail: string
        ├── serviceId: string
        ├── serviceName: string
        ├── vehicleInfo: object
        │   ├── make: string
        │   ├── model: string
        │   ├── year: number
        │   └── plateNumber: string
        ├── appointmentDate: string
        ├── appointmentTime: string
        ├── paymentDetails: object
        │   ├── method: string
        │   └── amount: number
        ├── receiptUrl: string
        ├── status: string
        ├── emissionTestResult: object
        │   ├── co2Level: string
        │   ├── smokeOpacity: string
        │   └── result: string
        ├── rejectionReason: string
        ├── approvedBy: string
        ├── approvedAt: timestamp
        ├── createdAt: timestamp
        └── updatedAt: timestamp
```

---

## 🔄 Component Interaction

### Frontend Component Hierarchy

```
App
│
├── Navbar
│   ├── Logo/Brand
│   ├── Navigation Links
│   └── Auth Buttons
│
├── Router
│   │
│   ├── Public Routes
│   │   ├── Home
│   │   ├── Login
│   │   ├── Register
│   │   └── Services
│   │
│   ├── Protected Routes (PrivateRoute)
│   │   ├── BookAppointment
│   │   ├── MyAppointments
│   │   └── AppointmentDetails
│   │
│   └── Admin Routes (AdminRoute)
│       ├── AdminDashboard
│       └── AdminAppointments
│
└── ToastContainer (Notifications)
```

---

## 🎯 User Actions Map

### What Users Can Do on Each Page

```
┌───────────────────────────────────────────┐
│              HOME PAGE                     │
├───────────────────────────────────────────┤
│ • Read about system                       │
│ • See features                            │
│ • Click "Book Appointment" → Services     │
└───────────────────────────────────────────┘

┌───────────────────────────────────────────┐
│            SERVICES PAGE                   │
├───────────────────────────────────────────┤
│ • View all services                       │
│ • See prices and durations                │
│ • Click "Book Now" → Login/Book          │
└───────────────────────────────────────────┘

┌───────────────────────────────────────────┐
│           LOGIN/REGISTER                   │
├───────────────────────────────────────────┤
│ • Enter credentials                       │
│ • Submit form                             │
│ • Switch between login/register           │
└───────────────────────────────────────────┘

┌───────────────────────────────────────────┐
│         BOOK APPOINTMENT                   │
├───────────────────────────────────────────┤
│ • Fill vehicle information                │
│ • Select date and time                    │
│ • Choose payment method                   │
│ • Upload receipt (optional)               │
│ • Submit booking                          │
└───────────────────────────────────────────┘

┌───────────────────────────────────────────┐
│         MY APPOINTMENTS                    │
├───────────────────────────────────────────┤
│ • View all appointments                   │
│ • Filter by status                        │
│ • Click appointment → Details             │
└───────────────────────────────────────────┘

┌───────────────────────────────────────────┐
│       APPOINTMENT DETAILS                  │
├───────────────────────────────────────────┤
│ • View full appointment info              │
│ • Upload receipt (if not done)            │
│ • View rejection reason (if rejected)     │
│ • View test results (if completed)        │
│ • Print results                           │
└───────────────────────────────────────────┘

┌───────────────────────────────────────────┐
│          ADMIN DASHBOARD                   │
├───────────────────────────────────────────┤
│ • View statistics                         │
│ • See appointment counts                  │
│ • Navigate to appointment management      │
└───────────────────────────────────────────┘

┌───────────────────────────────────────────┐
│        ADMIN APPOINTMENTS                  │
├───────────────────────────────────────────┤
│ • View all appointments                   │
│ • Filter by status                        │
│ • View payment receipts                   │
│ • Approve appointments                    │
│ • Reject appointments (with reason)       │
│ • Add test results                        │
└───────────────────────────────────────────┘
```

---

## 📱 Responsive Design Breakpoints

```
┌─────────────────────────────────────┐
│          MOBILE VIEW                 │
│         (< 768px)                    │
├─────────────────────────────────────┤
│ • Single column layout              │
│ • Stacked navigation                │
│ • Full-width cards                  │
│ • Touch-friendly buttons            │
└─────────────────────────────────────┘
          │
          │ Screen expands
          ▼
┌─────────────────────────────────────┐
│          TABLET VIEW                 │
│        (768px - 1024px)             │
├─────────────────────────────────────┤
│ • 2-column grids                    │
│ • Horizontal navigation             │
│ • Medium-sized cards                │
└─────────────────────────────────────┘
          │
          │ Screen expands
          ▼
┌─────────────────────────────────────┐
│         DESKTOP VIEW                 │
│          (> 1024px)                  │
├─────────────────────────────────────┤
│ • Multi-column layouts              │
│ • Full navigation bar               │
│ • Optimized card sizes              │
│ • Sidebar possibilities             │
└─────────────────────────────────────┘
```

---

## 🎨 Color Coding Reference

### Status Colors Throughout the System

```
┌────────────┬──────────┬─────────────────┐
│   Status   │  Color   │     Meaning     │
├────────────┼──────────┼─────────────────┤
│  Pending   │  Yellow  │ Awaiting action │
│  Approved  │  Green   │ Confirmed       │
│  Rejected  │  Red     │ Not approved    │
│  Completed │  Blue    │ Finished        │
└────────────┴──────────┴─────────────────┘
```

### Button Colors

```
┌─────────────┬──────────┬─────────────────┐
│   Button    │  Color   │      Use        │
├─────────────┼──────────┼─────────────────┤
│  Primary    │  Blue    │ Main actions    │
│  Success    │  Green   │ Approve/Confirm │
│  Danger     │  Red     │ Reject/Delete   │
│  Secondary  │  Gray    │ Cancel/Back     │
└─────────────┴──────────┴─────────────────┘
```

---

## 🔗 Navigation Paths

### All Possible Routes

```
PUBLIC ROUTES:
/                          → Home
/login                     → Login
/register                  → Register
/services                  → Services List

USER ROUTES (Protected):
/book-appointment/:id      → Booking Form
/my-appointments           → User's Appointments
/appointment/:id           → Appointment Details

ADMIN ROUTES (Admin Only):
/admin/dashboard           → Admin Dashboard
/admin/appointments        → Manage Appointments
```

---

## 📞 API Endpoint Map

```
BASE URL: http://localhost:5000/api

AUTH ENDPOINTS:
POST   /auth/register      → Register new user
POST   /auth/verify        → Verify token

USER ENDPOINTS:
POST   /appointments        → Create appointment
GET    /appointments/user   → Get user appointments
GET    /appointments/:id    → Get appointment details
PATCH  /appointments/:id/receipt → Upload receipt

ADMIN ENDPOINTS:
GET    /admin/appointments  → Get all appointments
GET    /admin/dashboard/stats → Get statistics
PATCH  /admin/appointments/:id/approve → Approve
PATCH  /admin/appointments/:id/reject → Reject
PATCH  /admin/appointments/:id/result → Add result

SERVICE ENDPOINTS:
GET    /services           → Get all services
GET    /services/:id       → Get service details
```

---

This visual guide provides a quick reference for understanding how the entire system works together! 🎨✨
